const {
  fetchJson
} = require("../myfunc")
global.fetchJson = fetchJson
//============ UrlApi =============//
global.urlApi = "https://api.catozolala.store"

/* Jangan di jual code ini karna ini gratis */